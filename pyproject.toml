[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "shadow"
version = "0.0.0"
description = "Deterministic quantitative research laboratory"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "websockets~=15.0",
]

[project.scripts]
shadow-live-data = "shadow.application.live_shadow:main"
shadow-paper-prepare = "shadow.application.paper_preparation:main"
shadow-paper-canary = "shadow.application.paper_canary:main"
shadow-crypto-capture = "shadow.application.crypto_capture:main"
shadow-crypto-paper = "shadow.application.crypto_paper:main"
shadow-btc-paper-session = "shadow.application.crypto_paper:session_main"
shadow-crypto-feed-relay = "shadow.operations.alpaca_crypto_feed_relay:main"

[dependency-groups]
dev = [
    "mypy>=1.18",
    "pytest>=8.3",
    "ruff>=0.12",
]

[tool.hatch.build.targets.wheel]
packages = ["src/shadow"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.mypy]
python_version = "3.12"
files = ["src", "tests"]
strict = true
warn_unused_configs = true
